# spring-jpa-jersey
Created to test my Skills in Spring, Data manipulation and JAX RS

Para rodar os testes, use o comando      mvn install
Para rodar a aplicação, use o comando    mvn spring-boot:run

Tecnologias utilizadas:

Java 8
Spring-data-rest
JPA
Spring 4 and Spring-boot
Maven
